{
  "name": "crypto-lab-aes-modes",
  "private": true,
  "version": "1.0.0",
  "description": "Interactive demonstration of AES block cipher modes of operation — ECB, CBC, CTR, GCM, and CCM",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:a11y": "playwright test",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.62.1",
    "gh-pages": "^6.3.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@noble/ciphers": "^2.3.0"
  }
}
